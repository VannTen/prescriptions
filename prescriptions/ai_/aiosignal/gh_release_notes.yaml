units:
  wraps:
  - name: AiosignalGitHubReleaseNotesWrap
    type: wrap.GHReleaseNotes
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: aiosignal
    run:
      release_notes:
        organization: aio-libs
        repository: aiosignal
        tag_version_prefix: v
