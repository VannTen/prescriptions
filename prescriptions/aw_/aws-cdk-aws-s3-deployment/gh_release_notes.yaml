units:
  wraps:
  - name: AwsxCdkxAwsxS3xDeploymentGitHubReleaseNotesWrap
    type: wrap.GHReleaseNotes
    should_include:
      adviser_pipeline: true
    match:
      state:
        resolved_dependencies:
        - name: aws-cdk-aws-s3-deployment
    run:
      release_notes:
        organization: aws
        repository: aws-cdk
        tag_version_prefix: v
